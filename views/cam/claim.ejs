<!DOCTYPE html>
<html lang="en">
<head>
    <title>AMS Cam</title>
    <% include ../shared/head.ejs %>
</head>
<body>


<%  var node_header_menu= false;  %>
<% include ../shared/header.ejs %>


<div class="step welcome-step np">
    <div class="container">
        <form method="POST">
        
            <h3 class="mt bordered mb">
                Claim your Camera 
            </h3>
            
            <%if (typeof errors != 'undefined') { %> 
                <div class="alert alert-danger dmt alert-dismissable">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    <i class="fa fa-warning"></i> <strong>Error</strong>
                    <ul> 
                        <% for(var i=0; i<errors.length; i++) {%>
                            <%if (errors[i] != "")  { %> 
                                <li><%= errors[i] %></li>
                            <% } %>    
                        <% } %>
                    </ul>
                </div>
            <% }  %>
            
            <%if (typeof success != 'undefined') { %> 
                <div class="alert alert-success dmt alert-dismissable">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
                    <i class="fa fa-check"></i> <strong><%=success%></strong> 
                </div>
            <% }  %>
 
            <div class="in">
            
                <div class="alert alert-info">
                    <span class="fa fa-warning"></span> You need to claim your camera toward the American Meteor Society. 
                </div>

                <% if(config.wlan_mac == 'undefined' || config.lan_mac == 'undefined') {%>
                    <div class="alert alert-danger">
                        The WLAN MAC and LAN MAC are missing in the config file. Something went wrong. Please, contact us.
                    </div>
                <% } else { %>

                    <strong>Please, follow these instructions:</strong>
                    <ol>
                        <li>Go to your <a href="http://www.amsmeteors.org/members/cam/claim&lan_mac=<%=config.lan_mac%>&wlan_mac=<%=config.wlan_mac%>&org=<%=url%>" class="btn btn-sm btn-primary"><span class="fa fa-hand-o-right"></span> AMS Camera Claim page</a> on the American Meteor Society website (login required)<br/></li>
                        <li>Once your camera is claimed on the AMS Website, <strong>you will receive an email with further instructions</strong> (We will use the email associated to the AMS account).</li>
                    </ol>

                 <% } %>
                   
            </div>
        </form>
    </div>
</div>

<% include ../shared/footer.ejs %>
</body>
</html>